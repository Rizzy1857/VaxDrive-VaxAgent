<Window x:Class="VaxDrive.VaxDock.Views.RemediationCardView"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Remediation Card" Height="500" Width="600"
        WindowStartupLocation="CenterOwner">
    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
            <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>

        <TextBlock x:Name="RiskTitle" Grid.Row="0" FontSize="20" FontWeight="Bold" Margin="0,0,0,10" TextWrapping="Wrap"/>
        
        <TextBlock Text="What to do:" Grid.Row="1" FontWeight="SemiBold" FontSize="16" Margin="0,10,0,5"/>
        <ListBox x:Name="StepsList" Grid.Row="2" Margin="0,0,0,20">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <TextBlock Text="{Binding}" TextWrapping="Wrap" Margin="0,5"/>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>
        
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right">
            <Button Content="Escalate" Width="100" Margin="0,0,10,0" Click="Escalate_Click"/>
            <Button Content="Mark Resolved" Width="100" Click="Resolve_Click"/>
        </StackPanel>
    </Grid>
</Window>
